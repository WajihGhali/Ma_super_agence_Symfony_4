{%  extends 'base.html.twig' %}
{%  block title "Agence Ghalin's" %}
{% block body %}
    <div class="jumbotron text-center">
        <h1>Agence Ghalin's</h1>
        <p>Vous êtes à la recherche d'une agence immobiliere à Sousse? Trouvez et comparez les prix des meilleures propriétés en vente issues de nos agences</p>


    </div>

    <div class="container" >
        <h2>Les Derniers Biens</h2>
        <div class="row flex">
            {% for property in properties %}
                <div class="col -3">
                    <div class="card">
                        <div class="col-md-8">
                            {% if property.filename %}
                                <img src="/images/properties/{{ property.filename }} " alt="card-img-top" style="width: 100%; height: auto;">
                            {% else %}
                                <img src="{{ '/images/properties/empty.jpeg'}}" alt="card-img-top" style="width: 100%; height: auto;">
                            {% endif %}
                        </div>
                        <div class="card-body">
                            <h5 class="card-title">
                                <a href="{{path('property.show' , {id : property.id , slug : property.slug})}}" >{{ property.title }}</a>
                            </h5>
                            <p class="card-text">{{ property.city }} ({{ property.postalCode }})</p>
                            <div class="text-primary" style="font-weight: bold ;  font-size: 2rem;">{{ property.formattedPrice }} DT</div>

                        </div>

                    </div>

                </div>
            {% endfor %}

        </div>
    </div>

{% endblock %}